LineCounting.py

Doesn't count blank lines
Doesn't count lines with just braces
Doesn't count the close of a class definition
Doesn't count comments (single, block)

Runs a single file
Iterates over all files in a directory

Keeps track of number of files, number of lines of code, number of lines with comments